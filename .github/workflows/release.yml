name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build-musl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      # aws-lc-sys (rustls) needs the musl cross toolchain
      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build musl binary
        run: |
          cargo build --release --target x86_64-unknown-linux-musl
          strip target/x86_64-unknown-linux-musl/release/reposcope

      - name: Package
        run: |
          set -euo pipefail
          mkdir -p dist
          name="reposcope-${GITHUB_REF_NAME}-x86_64-unknown-linux-musl"
          cd target/x86_64-unknown-linux-musl/release
          tar czf "../../../dist/${name}.tar.gz" reposcope
          cd ../../../dist
          sha256sum "${name}.tar.gz" | awk '{print $1}' > "${name}.tar.gz.sha256"
          ls -la

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
